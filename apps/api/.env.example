# Database
DATABASE_URL=postgresql://cartpick:cartpick@localhost:5433/cartpick

# App
PORT=3000
NODE_ENV=development

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3001

# Admin
ADMIN_EMAIL=admin@cartpick.app
ADMIN_PASSWORD=changeme123!

# JWT (admin)
JWT_SECRET=dev-jwt-secret-change-in-production
JWT_EXPIRY=8h

# User JWT (access + refresh)
USER_JWT_SECRET=dev-user-jwt-secret-change-in-production
USER_ACCESS_EXPIRY=30m
USER_REFRESH_EXPIRY_DAYS=30
